class srat:
	def __init__(self):
		self.questions = []
		#q1
		def c(self, q, qdict):
			for i in range(1, 21):
				qi = qdict[i]
				if qi.answer == 'b':
					return ord(q.answer) - 96 == i
		questions += question(question, c, 'a')