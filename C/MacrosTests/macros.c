#define until(exp) while(!(exp))
#define unless(exp) if(!(exp))
#define while(exp) while(exp){
#define end }

#include <stdio.h>

#define test define

int main() {
	while(1)
		printf(" saldkjf");
	end
	return 0;
}